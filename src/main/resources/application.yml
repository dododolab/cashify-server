spring:
  application:
    name: cashify-server

  datasource:
    url: jdbc:mysql://localhost:3306/cashify
    username: myuser
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
